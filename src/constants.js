const boardHeight = 18
const boardWidth = 10
const nextWidth = 4
const nextHeight = 4

export const boardSize = {boardWidth:boardWidth, boardHeight: boardHeight}
export const nextSize = {nextWidth: nextWidth, nextHeight:nextHeight}

